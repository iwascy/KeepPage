# Mockups

This directory stores static mockups, exported HTML prototypes, and visual references for KeepPage.

Mockups are documentation and design references. Production behavior should be implemented in `apps/web`, `apps/api`, or `apps/extension` as appropriate.
