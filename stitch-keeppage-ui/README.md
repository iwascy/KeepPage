# Stitch KeepPage UI Reference

This directory contains stitched UI experiments and generated design references.

Use it as design context only. Production frontend implementation belongs in `apps/web`, and extension UI belongs in `apps/extension`.
