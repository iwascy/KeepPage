# KeepPage Demo Assets

This directory contains standalone demo assets and local exploration material. It is not part of the production web, API, or extension runtime.

Production code lives under `apps/`. Treat files here as reference material unless a task explicitly asks to update demo assets.
